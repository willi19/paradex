paradex.io.camera_system.remote_camera_controller
=================================================

.. py:module:: paradex.io.camera_system.remote_camera_controller






Module Contents
---------------

.. py:data:: logger

.. py:class:: remote_camera_controller(name, pc_list=None, auto_reload=False, stall_timeout=3.0)

   Main-PC driver for a cluster of capture-PC camera daemons.

   Orchestrates the per-PC ``server_daemon`` processes over ZMQ REQ/REP:
   a background thread (:meth:`run`) opens a command socket per PC, then
   ticks a heartbeat loop that pushes ``start`` / ``stop`` / ``reload`` /
   ``end`` commands and folds each daemon's reply into a live health view
   (per-PC status plus per-camera frame-id stall detection). Public
   ``start`` / ``stop`` methods only flip :class:`threading.Event` objects
   that the loop consumes, so the caller never touches the sockets directly.

   :param name: Controller label; a ``_YYYYmmdd_HHMMSS`` timestamp is appended to make
                the unique ``controller_name`` sent with every command.
   :type name: str
   :param pc_list: Capture PCs to drive. Defaults to ``get_pc_list()`` when ``None``.
   :type pc_list: list of str, optional
   :param auto_reload: If ``True``, reload the cameras automatically (throttled) whenever a
                       persistent error or stall is detected, by default ``False``.
   :type auto_reload: bool, optional
   :param stall_timeout: Seconds without a new frame id before a camera is flagged as stalled,
                         by default 3.0.
   :type stall_timeout: float, optional


   .. py:attribute:: name


   .. py:attribute:: pc_list


   .. py:attribute:: ping_port
      :value: 5480



   .. py:attribute:: monitor_port
      :value: 5481



   .. py:attribute:: command_port
      :value: 5482



   .. py:attribute:: exit_event


   .. py:attribute:: start_event


   .. py:attribute:: stop_event


   .. py:attribute:: sending_event


   .. py:attribute:: error_event


   .. py:attribute:: init_error
      :value: None



   .. py:attribute:: auto_reload
      :value: False



   .. py:attribute:: stall_timeout
      :value: 3.0



   .. py:attribute:: capturing
      :value: False



   .. py:attribute:: continuous
      :value: False



   .. py:attribute:: cam_progress


   .. py:attribute:: stalled


   .. py:attribute:: pc_status


   .. py:attribute:: last_response


   .. py:attribute:: pc_last_seen


   .. py:attribute:: pub_timeout
      :value: 2.0



   .. py:attribute:: status_lock


   .. py:attribute:: health_thread
      :value: None



   .. py:attribute:: cmd_queue


   .. py:attribute:: worker_threads
      :value: []



   .. py:attribute:: worker_stop


   .. py:attribute:: run_thread


   .. py:method:: initialize()

      Connect a command socket to every reachable capture PC and register.

      Pings each PC in ``pc_list``, opens a non-blocking ZMQ ``REQ`` command
      socket to the ones that answer, seeds the per-PC error/status tables,
      then calls :meth:`register` to claim the daemons. Runs once from
      :meth:`run` at thread start.

      :raises ConnectionError: If any PC fails to respond to the ping (its ``server_daemon`` is
          not running).



   .. py:method:: check_server_alive(pc)

      Ping a capture PC's daemon and report whether it is up.

      ping port로 서버 확인. Sends ``"ping"`` on the ping port and expects
      ``"pong"`` back within the socket timeout.

      :param pc: Capture-PC name to probe.
      :type pc: str

      :returns: ``True`` if the daemon replied ``"pong"``, ``False`` on any ZMQ
                error or mismatched reply.
      :rtype: bool



   .. py:method:: send_command(cmd)

      Broadcast ``cmd`` to every PC via its worker and collect the replies.

      Enqueues the command on each per-PC worker (which owns the socket) and
      waits until all have replied. A slow PC delays only *this* call's return,
      not other PCs' keepalive. Same signature/return as before.

      :param cmd: Command payload with at least an ``action`` key; sent as JSON.
      :type cmd: dict

      :returns: Maps each PC name to its JSON reply (or a synthesized
                ``{'status': 'error', ...}`` dict on timeout / no worker reply).
      :rtype: dict



   .. py:method:: register()

      Claim the daemons for this controller by sending a ``register`` command.



   .. py:method:: start(mode, syncMode, save_path=None, fps=30, exposure_time=None, gain=None)

      Begin a capture across all PCs; blocks until the command is sent.

      Stores the capture parameters, then signals :meth:`run` via
      ``start_event`` and waits on ``sending_event`` for the loop to actually
      dispatch the ``start`` command to the daemons (the handshake).

      :param mode: ``image`` / ``video`` / ``stream`` / ``full``.
      :type mode: str
      :param syncMode: Use the hardware trigger if ``True``.
      :type syncMode: bool
      :param save_path: Output dir/file for ``video`` / ``image`` modes.
      :type save_path: str, optional
      :param fps: Frame rate for free-run capture, by default 30.
      :type fps: int, optional
      :param exposure_time: Per-capture overrides; ``None`` keeps each camera's baseline.
      :type exposure_time: float, optional
      :param gain: Per-capture overrides; ``None`` keeps each camera's baseline.
      :type gain: float, optional



   .. py:method:: stop()

      Stop the current capture; blocks until the stop command is sent.

      Signals :meth:`run` via ``stop_event`` and waits on ``sending_event``
      for the loop to dispatch the ``stop`` command to every daemon.



   .. py:method:: end()

      Shut down the controller and join the background thread.

      Sets ``exit_event`` so :meth:`run` breaks its loop (sending a final
      ``end`` command and closing the sockets/context), then joins the thread.



   .. py:method:: reload_cameras()

      Ask every daemon to reload its cameras; flag errors.

      Sends a ``reload`` command to all PCs and sets ``error_event`` for any
      PC whose reply reports an error.



   .. py:method:: force_takeover()

      다른 controller 가 lock 잡고 있어도 강제로 register 재시도.



   .. py:method:: arm(syncMode=False, fps=30, exposure_time=None, gain=None)

      Start acquiring with **no** sink; toggle sinks later with
      :meth:`set_record` / :meth:`set_stream` / :meth:`snapshot`.

      Convenience for ``start("acquire", ...)`` — the decoupled-sink entry point.
      Cameras grab frames continuously; nothing is written or streamed until a
      sink is enabled.



   .. py:method:: set_sink(video=None, stream=None, save_path=None, snapshot=None)

      Toggle output sinks on the running capture — any time, any combination.

      Thread-safe (goes through the per-PC workers). Only the given sinks change.

      :param video: Turn the ``.avi`` video sink on/off.
      :type video: bool, optional
      :param stream: Turn the shared-memory stream sink on/off.
      :type stream: bool, optional
      :param save_path: Session-relative dir for the video sink (used when it turns on).
      :type save_path: str, optional
      :param snapshot: ``(save_path, count)`` — write the next ``count`` frames as images.
      :type snapshot: tuple, optional

      :returns: Per-PC reply from the ``sink`` command.
      :rtype: dict



   .. py:method:: set_param(gain=None, exposure=None)

      Change gain/exposure on the running capture **live** (no restart).

      Each of ``gain`` / ``exposure`` may be a scalar (all cameras) or a
      ``{serial: value}`` dict (per-camera). Used by the live tuner.

      :param gain: New gain in dB — scalar or ``{serial: dB}``.
      :type gain: float or dict, optional
      :param exposure: New exposure in microseconds — scalar or ``{serial: us}``.
      :type exposure: float or dict, optional

      :returns: Per-PC reply from the ``param`` command.
      :rtype: dict



   .. py:method:: set_record(save_path=None, on=True)

      Turn the video (.avi) sink on (with ``save_path``) or off, live.



   .. py:method:: set_stream(on=True)

      Turn the shared-memory stream sink on/off, live.



   .. py:method:: snapshot(save_path, count=1)

      Write the next ``count`` frames from every camera as images.



   .. py:method:: run()


   .. py:method:: is_error()

      True if any PC is currently erroring/stalled (live) or a capture was
      interrupted (sticky, until the next :meth:`start`).



   .. py:method:: capture_interrupted()

      True if a daemon died/restarted mid-capture and the recording was not
      resumed. Sticky until the next :meth:`start` / :meth:`arm`.



   .. py:method:: get_status()

      Live health snapshot for the caller to react to.

      :returns: ``{'error': bool, 'stalled': [cam, ...], 'pc': {pc: {'status', 'msg'}}}``
      :rtype: dict



