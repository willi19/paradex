paradex.process.processor
=========================

.. py:module:: paradex.process.processor

.. autoapi-nested-parse::

   Core implementation of the paradex.process batch framework.

   See :mod:`paradex.process` for the high-level overview and usage example.











Module Contents
---------------

.. py:data:: CACHE_ROOT

.. py:data:: STATUSES
   :value: ('pending', 'caching', 'processing', 'uploading', 'completed', 'skipped', 'failed')


.. py:data:: DONE_STATUSES
   :value: ('completed', 'skipped', 'failed')


.. py:exception:: Skip

   Bases: :py:obj:`Exception`


   Raise inside ``process(job, ctx)`` to stop and mark the job ``skipped``.


.. py:class:: Job

   One unit of work.

   .. attribute:: id

      Unique id. Used as the status key and the per-job cache subdir,
      so keep it filesystem-safe (slashes are fine, they nest dirs).

   .. attribute:: inputs

      ``{name: nas_relpath}`` — files pulled from NAS (``shared_dir``)
      into the local cache before ``process`` runs. Reach them with
      ``ctx.input(name)``.

   .. attribute:: outputs

      ``{name: nas_relpath}`` — files your ``process`` writes locally
      (path from ``ctx.output(name)``) and which are uploaded to NAS
      after it returns successfully.

   .. attribute:: meta

      Arbitrary extra data for your ``process`` fn (``job.meta[...]``).

   .. attribute:: done

      Optional ``(job) -> bool`` completion predicate. This is an
      intentional idempotency extension point for jobs whose real
      completion cannot be proven by output-file existence alone
      (for example multi-file manifests or external quality gates).
      When supplied it is authoritative; otherwise the conservative
      default skips only when *all* declared ``outputs`` already
      exist on NAS. For ``num_workers > 1`` the callable must be
      picklable, just like ``process_fn``.


   .. py:attribute:: id
      :type:  str


   .. py:attribute:: inputs
      :type:  Dict[str, str]


   .. py:attribute:: outputs
      :type:  Dict[str, str]


   .. py:attribute:: meta
      :type:  Dict[str, Any]


   .. py:attribute:: done
      :type:  Optional[Callable[[Job], bool]]
      :value: None



.. py:class:: Ctx(job: Job, progress_dict, cache_dir: str)

   Handle passed to ``process(job, ctx)``.

   Gives the transform its cached input paths, its output paths to write, and a
   ``status()`` reporter — nothing else. Caching, uploading, skipping and error
   capture are handled by the framework around it.


   .. py:attribute:: job


   .. py:method:: input(name: str) -> str

      Local path of a cached input (already downloaded from NAS).



   .. py:method:: output(name: str) -> str

      Local path where you should write output ``name`` (parent dir ensured).

      Whatever you write here is uploaded to ``job.outputs[name]`` on NAS after
      ``process`` returns.



   .. py:method:: status(message: str = '', progress: Optional[float] = None)

      Report progress. ``progress`` is 0..1 (stored as 0..100 percent).



.. py:class:: Processor(jobs: List[Job], process_fn: Callable, num_workers: int = 4, keep_cache: bool = False, on_update: Optional[Callable] = None, quiet: bool = False)

   Runs a batch of :class:`Job` through ``process_fn`` with caching, skip,
   upload, console reporting and per-job error capture.

   :param jobs: list of :class:`Job`.
   :param process_fn: ``(job, ctx) -> None`` — must be a top-level (picklable)
                      function when ``num_workers > 1``.
   :param num_workers: process-pool size. ``1`` runs **in-process** (no pool), which
                       is the easiest way to debug — real tracebacks, no pickling.
   :param keep_cache: keep per-job cache/output files after success (default drop).
   :param on_update: optional ``(progress: dict) -> None`` hook called each refresh
                     with ``{job_id: {status, progress, message, ...}}`` — feed it
                     to the ZMQ dashboard, a log, etc.
   :param quiet: suppress the built-in console table (e.g. when using
                 ``on_update`` for display).


   .. py:attribute:: jobs


   .. py:attribute:: process_fn


   .. py:attribute:: num_workers


   .. py:attribute:: keep_cache
      :value: False



   .. py:attribute:: on_update
      :value: None



   .. py:attribute:: quiet
      :value: False



   .. py:attribute:: progress


   .. py:attribute:: log


   .. py:method:: run(update_interval: float = 1.0)

      Process every job, blocking until done, refreshing status each interval.



   .. py:method:: stop()


   .. py:method:: get_progress() -> dict


   .. py:method:: summary() -> Dict[str, int]


   .. py:method:: failures() -> Dict[str, dict]


.. py:function:: run_jobs(jobs: List[Job], process_fn: Callable, num_workers: int = 4, keep_cache: bool = False, on_update: Optional[Callable] = None, quiet: bool = False) -> dict

   One-call helper: build a :class:`Processor`, run it, return the final progress.

   See :class:`Processor` for argument meanings.


