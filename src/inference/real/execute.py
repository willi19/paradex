# 



if __name__ == "__main__":
    # Set end pose
        # Load current object pos in camera coordinate
        # multiply inv(c2r) to move object pos from camera to robot coordinate
        # Load wrist position according to object pos
        # Multiply object pos with wrist pos to get the end pose
        
    # Set start pose
        # start pose is obj_pos + (wrist_pos - obj_pos) * n (n = 5)

    # Move to start pose
    # Move to end pose)

    # Move to grasp pose
    # Move to grasp action

    # Lift 
    # Move to right
    # Drop