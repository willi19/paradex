paradex.image.aruco
===================

.. py:module:: paradex.image.aruco

.. autoapi-nested-parse::

   ArUco and CharUco marker detection module (optimized version)
   - Caching for faster repeated detection
   - Safe handling of empty detections
   - Cleaner merging of multi-board results







Module Contents
---------------

.. py:data:: aruco_dict

.. py:data:: boardinfo_dict

.. py:function:: get_aruco_detector(dict_type: str)

.. py:function:: get_charuco_detector()

.. py:function:: detect_aruco(img, dict_type='6X6_1000') -> Tuple[List[numpy.ndarray], numpy.ndarray]

   Detect ArUco markers in a single image.

   :param img: Input image (BGR or grayscale).
   :type img: numpy.ndarray
   :param dict_type: Predefined dictionary key (e.g. ``'6X6_1000'``); see :data:`aruco_dict`.
   :type dict_type: str

   :returns: ``(corners, ids)``. ``corners`` is a list of ``(1, 4, 2)`` arrays and
             ``ids`` is an ``(N, 1)`` int array; returns ``([], (0, 1) zeros)`` when
             nothing is detected.
   :rtype: tuple


.. py:function:: check_boardinfo_valid(boardinfo)

.. py:function:: detect_charuco(img)

   Detect ChArUco board corners in a single image, per board.

   Runs every board's cached ``CharucoDetector`` (from ``charuco_info.json``) and
   omits boards with no detection.

   :param img: Input image.
   :type img: numpy.ndarray

   :returns: ``{board_id: {"checkerCorner": (n, 2), "checkerIDs": (n,)}}`` for each board
             that produced detections.
   :rtype: dict


.. py:function:: get_adjecent_ids()

.. py:function:: merge_charuco_detection(detection_list)

   Flatten per-board ChArUco detections into a single id-offset corner set.

   Concatenates every board's corners, offsetting each board's ids by the running
   total ``sum (numX-1)(numY-1)`` so ids stay globally unique across boards.

   :param detection_list: Output of :func:`detect_charuco` (``{board_id: {checkerCorner, checkerIDs}}``).
   :type detection_list: dict

   :returns: ``{"checkerCorner": (M, 2), "checkerIDs": (M,)}``; empty arrays if there were
             no detections.
   :rtype: dict


.. py:function:: draw_charuco(image, corners, color=(0, 255, 255), radius=4, thickness=2, ids=None)

.. py:function:: draw_aruco(img, kypt, ids=None, color=(255, 0, 0))

.. py:function:: draw_keypoint(img, kypt, color=(255, 0, 0))

.. py:function:: find_common_indices(ids1, ids2)

   Find indices such that corners can be directly matched.

   Usage:
       idx1, idx2 = find_common_points(ids1, ids2)
       matched_cor1 = cor1[idx1]
       matched_cor2 = cor2[idx2]
       # Now matched_cor1[i] corresponds to matched_cor2[i]

   :returns: indices for array1
             idx2: indices for array2
             (Both sorted by ID to ensure correspondence)
   :rtype: idx1


.. py:function:: get_board_cor()

   Return ChArUco board object points, keyed by board id.

   Corners come from ``board.getChessboardCorners()`` (in checker-length units) and
   are scaled by ``0.05``. This scale is board-``"3"``-specific: it yields 5 cm
   squares only for boards whose ``checkerLength == 1.0``; boards already in metric
   units would be double-scaled.

   :returns: ``{board_id: {"checkerIDs": (N,), "checkerCorner": (N, 3)}}``.
   :rtype: dict


